# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.createGet
POST    /                           controllers.Application.createPost

GET     /vote/:name                 controllers.Application.voteGet(name)

GET     /result/:name               controllers.Application.result(name)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
