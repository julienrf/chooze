# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Chooze.index
POST    /                           controllers.Chooze.createPoll

GET     /:name/vote                 controllers.Chooze.showVoteForm(name)
POST    /:name/vote                 controllers.Chooze.vote(name)

GET     /:name/result               controllers.Chooze.result(name)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
